# Session Memory Dump — 001
**Date:** 2026-06-24  
**Tool:** Claude Code (claude-sonnet-4-6)

---

## 1. Azure Authentication

- Logged in via `az login` (browser-based auth)
- Account: `saikat.bhattacharjee14@outlook.com`
- Tenant ID: `32233fea-ba06-4cef-a970-59f8be3d5d04`
- Active subscription switched to: **Saikat_Subscription** (`d56ace31-d353-4f3c-8944-4673522b75ac`)

---

## 2. Azure Resources Discovered

**Resource Group:** `rg-claude-ind` — Location: `centralindia`

| Resource Name | Type |
|---|---|
| `adfclaude01` | Azure Data Factory |
| `adlsclaude01` | Azure Storage Account (ADLS) |
| `akvclaude01` | Azure Key Vault |
| `asqlserverclaude01` | Azure SQL Server |
| `asqlserverclaude01/master` | SQL Database (system) |
| `asqlserverclaude01/asqlclaude01` | SQL Database (project DB) |

---

## 3. Project Goal

Build a **metadata-driven ingestion framework** on Azure with the following design principles:

- Metadata control tables stored in SQL Database `asqlclaude01`
- **Two ADF pipelines per data source:** Full Refresh + Incremental
- ADF uses **Lookup activities** to read metadata at runtime
- Linked services, datasets, and pipeline parameters are **dynamically populated** from metadata
- Working directory: `c:\Users\bhatt\OneDrive\Desktop\Azure_Claude`

---

## 4. Azure MCP Server Configuration

**File:** `.claude/settings.json`

```json
{
  "mcpServers": {
    "azure": {
      "command": "npx",
      "args": ["-y", "@azure/mcp@latest", "server", "start"],
      "env": {
        "AZURE_SUBSCRIPTION_ID": "d56ace31-d353-4f3c-8944-4673522b75ac",
        "AZURE_TENANT_ID": "32233fea-ba06-4cef-a970-59f8be3d5d04"
      }
    }
  }
}
```

- Package: `@azure/mcp` (version resolved: `3.0.0-beta.20`)
- Auth: uses existing `az login` credentials (no separate config needed)
- Scope: project-local (only active when `Azure_Claude` is the workspace)

---

## 5. Project Folder Structure Created

```
Azure_Claude/
├── .claude/
│   └── settings.json          # Azure MCP server config
├── adf/
│   ├── pipelines/             # ADF pipeline JSON definitions
│   ├── datasets/              # ADF dataset JSON definitions
│   └── linked_services/       # ADF linked service JSON definitions
├── memory/                    # Session memory dumps (this folder)
│   └── session_001_2026-06-24.md
├── metadata/                  # Metadata config files / exports
├── sql/
│   ├── ddl/                   # CREATE TABLE scripts for metadata tables
│   └── seed/                  # INSERT scripts to seed metadata
├── docs/                      # Architecture diagrams, design notes
└── CLAUDE.md                  # Project context loaded into every session
```

---

## 6. Immediate Next Steps (Planned)

- [ ] Design metadata table schema (DDL) in `sql/ddl/`
- [ ] Seed initial metadata rows for first data sources in `sql/seed/`
- [ ] Deploy DDL to `asqlclaude01`
- [ ] Build generic ADF Linked Services (SQL, ADLS, Key Vault)
- [ ] Build parameterised ADF Datasets
- [ ] Build master Full Refresh pipeline with Lookup → dynamic copy activity
- [ ] Build master Incremental pipeline with watermark logic
